name: CI/CD
on:
  push: { branches: [ master ] }
  pull_request: { branches: [ master ] }
  workflow_dispatch:

env:
  IMAGE: ion21/importer-service        # schimbă per serviciu

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { distribution: temurin, java-version: '17', cache: maven }
      - name: Build + unit + integration (Testcontainers)
        run: ./mvnw -B verify          # eșuează => oprește pipeline-ul

  publish:
    needs: build-test
    if: github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.meta.outputs.sha7 }}
    steps:
      - uses: actions/checkout@v4
      - id: meta
        run: echo "sha7=${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{ env.IMAGE }}:${{ steps.meta.outputs.sha7 }}
            ${{ env.IMAGE }}:latest

  cd-bump:                            # închide bucla CI -> CD
    needs: publish
    if: github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          repository: nimigeanconstantinion/ms-gitops
          ref: master
          token: ${{ secrets.GITOPS_PAT }}      # PAT least-privilege (vezi §6)
      - name: Bump image tag în gitops
        env:
          VALUES: business/rsk/importer-service/values.yaml
        run: |
          test -f "$VALUES" || { echo "::error::$VALUES nu există în ms-gitops — refactor nepushuit?"; exit 1; }
          sed -i -E "s|^(  tag: ).*|\1${{ needs.publish.outputs.tag }}|" "$VALUES"
          git config user.name  "ci-bot"
          git config user.email "ci-bot@users.noreply.github.com"
          if git diff --quiet; then
            echo "Tag deja ${{ needs.publish.outputs.tag }} — nimic de comis."
            exit 0
          fi
          git commit -am "ci: importer-service -> ${{ needs.publish.outputs.tag }}"
          git push